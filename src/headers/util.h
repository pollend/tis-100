#ifndef _UTIL_H
#define _UTIL_H

//void raise_error(const char *msg, ...);
char *trim_whitespace(char *str);

void custom_log(const char *msg, ...);
char* str_dump(const char*);
char* combine_str(char* str1,char* str2);


#endif
